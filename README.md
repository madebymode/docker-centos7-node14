# docker-centos7-node14

build command

```
docker build --build-arg  HOST_USER_UID=$(id -u) --build-arg  HOST_USER_UID=$(id -g)  github.com/madebymode/docker-centos7-node14.git -t madebymode/node14
```
